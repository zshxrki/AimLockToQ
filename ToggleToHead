local cameraToggle = false
local cameraTarget = nil

-- Массив с названиями частей тела для R6
local r6Parts = {"Head", "Torso", "Right Arm", "Left Arm", "Right Leg", "Left Leg"}

local function findClosestPlayerToScreenCenter()
    local playerList = game.Players:GetPlayers()
    local camera = workspace.CurrentCamera
    local center = Vector2.new(camera.ViewportSize.X / 2, camera.ViewportSize.Y / 2)
    local closestPlayer = nil
    local minDistance = math.huge
    local localPlayer = game.Players.LocalPlayer

    for _, player in pairs(playerList) do
        if player ~= localPlayer and player.Character and player.Character:FindFirstChild("HumanoidRootPart") then
            local screenPos, onScreen = camera:WorldToScreenPoint(player.Character.HumanoidRootPart.Position)
            
            if onScreen then
                local distance = (center - Vector2.new(screenPos.X, screenPos.Y)).Magnitude
                if distance < minDistance then
                    minDistance = distance
                    closestPlayer = player
                end
            end
        end
    end

    return closestPlayer
end

local function aimCameraAtPlayer(player)
    if player and player.Character then
        local camera = workspace.CurrentCamera
        local center = Vector2.new(camera.ViewportSize.X / 2, camera.ViewportSize.Y / 2)
        
        local closestPart = nil
        local minDistance = math.huge
        
        -- Перебираем все части тела R6, чтобы найти ближайшую к центру экрана
        for _, partName in pairs(r6Parts) do
            local part = player.Character:FindFirstChild(partName)
            if part then
                local screenPos, onScreen = camera:WorldToScreenPoint(part.Position)
                if onScreen then
                    local distance = (center - Vector2.new(screenPos.X, screenPos.Y)).Magnitude
                    if distance < minDistance then
                        minDistance = distance
                        closestPart = part
                    end
                end
            end
        end

        -- Если деталь найдена, наводим на неё камеру
        if closestPart then
            camera.
